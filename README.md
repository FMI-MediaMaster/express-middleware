**Provides helpers for express responses**

```js
import express from 'express';
import responseHelpers from '@FMI-MediaMaster/express-responses';

const app = express();
app.use(responseHelpers);
```
